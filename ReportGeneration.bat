@echo off
allure serve test-outputs/allure-results
pause
exit



// comment : This batch file generates and serves an Allure report from the test results located in the "test-outputs/allure-results" directory. It pauses the command prompt after serving the report to allow the user to view any messages before exiting.